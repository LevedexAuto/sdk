// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract LevedexExecutor {
    address public owner;

    constructor() {
        owner = msg.sender;
    }

    function executeTrade(
        address tokenIn,
        address tokenOut,
        uint256 amount
    ) external {
        require(msg.sender == owner, "Unauthorized");
        // DEX interaction logic here
    }
}
